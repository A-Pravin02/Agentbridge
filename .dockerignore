node_modules
**/node_modules
**/dist
**/.next
**/*.tsbuildinfo
.git
.github
graphify-out
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.*
!.env.example
apps/api/.demo-agent.json
# The API image does not need the dashboard's source — but `npm ci` resolves the
# whole workspace from the lockfile, so its package.json must be present or the
# install fails. Exclude the contents, keep the manifest.
apps/web/**
!apps/web/package.json
docs
*.md
